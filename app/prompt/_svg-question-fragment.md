SVG prompt kuralı: "svg_prompt" opsiyonel — sadece görsel olmadan soru tam anlaşılmıyorsa (sayı doğrusu, geometrik şekil, kesir/oran, grafik) doldur, yoksa null. {lesson} gibi derslerde daha sık gerekebilir (matematik/geometri/fen), sosyal bilgiler/din kültüründe genelde gerekmez; hiç olmaması normal. svg_prompt DÜZ METİN bir talimat olmalı — ne çizileceğini anlat, SVG/XML kodu YAZMA (kodu başka bir AI yazacak). Talimatında şunları söyle: viewBox'lı `<svg>` kökü kullansın; script/style/foreignObject/image/a/use/gradient/filter/animasyon/event-handler kullanmasın; renk kodlarını "#" ile başlatsın; metni `<text>` içinde tutsun; sade 3-4 renk kullansın, farklı unsurları (gövde/çizgi/etiket) ayırt etsin, çakışan çizgileri aynı renkte bırakmasın; kontrastlı zemin/yazı kullansın; sadece SVG kodu döndürsün. "svg_position": "yukarıdaki/aşağıdaki şekle göre" ifadesine göre, yoksa mantığa göre "above"/"below" (null ise "above").
